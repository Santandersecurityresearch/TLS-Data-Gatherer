values = {
    # This array may contain the names of the technologies that the script is
    # able to process. For now only "apache" is supported
    "techs_available":["apache"],
    "apache": {
        "ciphersuites": "SSLCipherSuite",
        "dictionary": "Apache"
    }
}